# Evidence records

No Plan 017 target record has been captured yet. Add one JSON record per
completed run, list its relative path in the parent manifest, and keep its raw
artifacts under ignored `tools/out/plan017_decoder_mip_acceptance/`.

Do not add discovery-only, planned, or inferred records. Those states belong in
the manifest as `not run` or `blocked`.
