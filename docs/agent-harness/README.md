# Agent harness

The canonical entry point is [`index.md`](index.md). This compatibility route
remains because older repository documents link to `README.md`.

The harness is repository-owned. The globally installed adoption skill is an
independent authoring and verification aid, not a runtime or CI dependency.
