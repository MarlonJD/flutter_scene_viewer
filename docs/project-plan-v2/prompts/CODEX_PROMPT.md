# Codex Başlatma Promptu

Repo kökündeki `START_HERE.md`, `AGENTS.md`, tüm `docs/`, `planning/task_graph.yaml` ve `prompts/MASTER_PROMPT.txt` dosyalarını oku.

Ardından `MASTER_PROMPT.txt` kurallarını bağlayıcı kabul ederek yalnızca ilk tamamlanmamış milestone'u uygula.

İlk çalışmada M0 dışına çıkma. Exact Flutter ve `flutter_scene` revision'ını sabitle, upstream API audit notunu yaz ve network GLB + runtime PBR/texture + hierarchy/raycast + request-frame spike'larını çalıştır. Ham `flutter_gpu` renderer, GLSL, tessellation, UV generation, animation veya compression ekleme.

Her adımda analyzer/test çalıştır ve sonunda MASTER_PROMPT'taki yedi maddelik çalışma raporunu ver.
