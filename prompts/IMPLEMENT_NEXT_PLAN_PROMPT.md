# Implement next plan prompt

Read `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, and the selected active plan. Execute
only that plan. Keep changes surgical. Update tests and docs. Run
`bash harness/run_checks.sh`. Update the plan progress log before stopping.

Selected plan:

```text
docs/exec-plans/active/<PLAN>.md
```
